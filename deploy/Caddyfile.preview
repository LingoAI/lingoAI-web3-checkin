{
	admin off
	auto_https off
}

:8080 {
	root * /srv
	encode gzip
	header {
		Cache-Control "no-store"
		X-Robots-Tag "noindex, nofollow, noarchive"
		X-Content-Type-Options "nosniff"
	}
	file_server
}
